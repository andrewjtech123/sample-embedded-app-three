import * as React from 'react';
import { classNames } from '@shopify/react-utilities/styles';
import * as styles from '../../Layout.scss';
export default function Section({ children, secondary, fullWidth }) {
    const className = classNames(styles.Section, secondary && styles['Section-secondary'], fullWidth && styles['Section-fullWidth']);
    return <div className={className}>{children}</div>;
}
