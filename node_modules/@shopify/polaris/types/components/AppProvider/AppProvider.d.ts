import * as React from 'react';
import { AppProviderProps, Context } from './types';
export default class AppProvider extends React.Component<AppProviderProps> {
    static childContextTypes: React.ValidationMap<any>;
    polarisContext: Context;
    private stickyManager;
    private scrollLockManager;
    private subscriptions;
    constructor(props: AppProviderProps);
    componentDidMount(): void;
    componentWillReceiveProps({ i18n, linkComponent, apiKey, shopOrigin, forceRedirect, }: AppProviderProps): void;
    getChildContext(): Context;
    render(): JSX.Element;
    subscribe(callback: () => void): void;
    unsubscribe(callback: () => void): void;
}
