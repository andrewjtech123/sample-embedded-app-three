import { State } from './DataTable';
interface TableMeasurements {
    fixedColumnWidth: number;
    firstVisibleColumnIndex: number;
    tableLeftVisibleEdge: number;
    tableRightVisibleEdge: number;
}
export declare function measureColumn(tableData: TableMeasurements): (column: HTMLElement, index: number) => {
    leftEdge: number;
    rightEdge: number;
    isVisible: boolean;
};
export declare function isEdgeVisible(position: number, start: number, end: number): boolean;
export declare function getPrevAndCurrentColumns(tableData: TableMeasurements, columnData: State['columnVisibilityData']): {
    previousColumn: import("./DataTable").ColumnVisibilityData;
    currentColumn: import("./DataTable").ColumnVisibilityData;
};
export {};
