import { RGBColor, RGBAColor } from '../components';
export declare function isLight({ red, green, blue }: RGBColor | RGBAColor): boolean;
export declare function isDark(color: RGBColor | RGBAColor): boolean;
